version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd todo-list
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /todo-list/.next
    files:
      - '**/*'
  cache:
    paths: []